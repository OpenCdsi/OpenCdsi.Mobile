<?xml version="1.0" encoding="utf-8"?>
<configuration>
  <packageSources>
    <add key="nuget.org" value="https://api.nuget.org/v3/index.json" />
    <add key="opencdsi-github" value="https://nuget.pkg.github.com/OpenCdsi/index.json" />
  </packageSources>

  <!--
    GitHub Packages requires authentication to restore NuGet packages even when both the
    package and its source repository are public. Set these two env vars before restoring:

      GITHUB_ACTOR   - your GitHub username
      GITHUB_TOKEN    - a PAT (classic or fine-grained) with read:packages scope,
                        or, in a GitHub Actions workflow, the built-in ${{ secrets.GITHUB_TOKEN }}
  -->
  <packageSourceCredentials>
    <opencdsi-github>
      <add key="Username" value="%GITHUB_ACTOR%" />
      <add key="ClearTextPassword" value="%GITHUB_TOKEN%" />
    </opencdsi-github>
  </packageSourceCredentials>
</configuration>
